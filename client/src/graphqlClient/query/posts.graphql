query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    totalCount
    cursor
    hasMore
    paginatedPosts {
      id
      title
      text
      textSnippet
      createdAt
      user {
        ...userInfo
      }
    }
  }
}
